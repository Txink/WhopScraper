# 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# ============================================================
# Parser层输出控制
# ============================================================

# 是否显示Parser层的指令解析输出
# 设置为 false 可以在broker下单时避免输出冲突
# 默认值: true
# 可选值: true, false, 1, 0, yes, no
SHOW_PARSER_OUTPUT=true

# ============================================================
# Broker层配置（待实现）
# ============================================================

# 价格偏差容忍度（百分比）
# 当市场价格与目标价格的偏差小于此值时，允许以市场价买入
# 默认值: 5
PRICE_DEVIATION_TOLERANCE=5

# 小仓位数量
# 默认值: 10
POSITION_SIZE_SMALL=10

# 中仓位数量
# 默认值: 20
POSITION_SIZE_MEDIUM=20

# 大仓位数量
# 默认值: 50
POSITION_SIZE_LARGE=50
